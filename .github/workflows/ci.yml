name: CI

on:
  pull_request:
    branches: [develop]

permissions:
  contents: read

# A new push to the same PR cancels the run for the old push.
concurrency:
  group: ci-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  build:
    uses: ./.github/workflows/build.yml
